apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: todo-app
type: Opaque
stringData:
  jwt-key: "your-super-secret-jwt-key-change-this-in-production-minimum-32-characters-long"
---
# Docker Hub Secret for Kubernetes
# This secret should be created manually using kubectl:
#
# kubectl create secret docker-registry dockerhub-secret \
#   --docker-server=https://index.docker.io/v1/ \
#   --docker-username=hamayal497 \
#   --docker-password=<YOUR_DOCKERHUB_PASSWORD> \
#   --docker-email=<YOUR_EMAIL> \
#   --namespace=todo-app
#
# Or create it via GitHub Actions workflow automatically
